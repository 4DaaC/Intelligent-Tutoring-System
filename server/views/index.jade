h1= title
p Welcome to #{title}
 script
  function load() {
    $.post('/student', {username: 'joel'},
      function(data) {
        $('#result').append(data); 
      }
    );
  }
 #result
 button(onClick='load();') Click
